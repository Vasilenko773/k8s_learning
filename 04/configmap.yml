apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  default.conf: |
    server {
        listen 80;

        location / {
            root /;
            default_type text/plain;
            try_files /storage/time.log =404;
        }
    }
